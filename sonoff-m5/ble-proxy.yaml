#Bluetooth proxy
esp32_ble_tracker:
  scan_parameters:
    # For WiFi-based proxies, changing the interval or window from their 
    # default values may result in an unstable WiFi connection. Using the 
    # default values for interval and window will usually resolve any instability.
    # interval: 1100ms
    # window: 1100ms
    active: false
bluetooth_proxy:
  # Active connections are now enabled by default
  # To disable active connections (previous default behavior), use:
  # active: false
  active: true